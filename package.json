{
  "name": "mi-web-canvas",
  "version": "1.0.0",
  "type": "module",
  "description": "Sistema para subir y gestionar Canvas para OpenTune",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/blob": "^0.25.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}